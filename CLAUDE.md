# CLAUDE.md

This project keeps its agent instructions in [`AGENTS.md`](AGENTS.md), the
cross-vendor standard, so that Claude Code, Codex, Copilot, Cursor and the rest
read the same file instead of three copies that drift apart.

**Read it before making changes.** It is imported below, so it loads
automatically as part of this file.

@AGENTS.md
