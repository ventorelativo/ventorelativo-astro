# Keystatic GitHub mode (Phase 3).
#
# You do not write these by hand. With `storage: { kind: 'github' }` in
# keystatic.config.ts, run `npm run dev`, open http://localhost:4321/keystatic
# and follow "create a GitHub App" — Keystatic creates the app, and writes the
# real values into a `.env` file next to this one.
#
# `.env` is git-ignored and must stay that way: the client secret is a
# credential. The same four values go into the host's environment settings for
# the deployed site.
KEYSTATIC_GITHUB_CLIENT_ID=
KEYSTATIC_GITHUB_CLIENT_SECRET=
KEYSTATIC_SECRET=
PUBLIC_KEYSTATIC_GITHUB_APP_SLUG=

# MapTiler (Phase 4b). Tiles, the club's custom style and terrain data.
# PUBLIC_, so it is compiled into the page and visible to visitors — that is
# how tile keys work. Protect it by restricting the referrer domains in the
# MapTiler dashboard, not by hiding it.
PUBLIC_MAPTILER_KEY=

# Tracestrack (Phase 4b). The map's raster topographic base layer.
# Same shape as the MapTiler key: public by necessity, protected by a referrer
# allowlist in the Tracestrack console, and kept out of the repository so a
# scraper does not find the club's key sitting in public source.
PUBLIC_TRACESTRACK_KEY=

# Cloudflare Web Analytics (S16). Optional — absent, no beacon is rendered.
PUBLIC_CF_BEACON_TOKEN=
